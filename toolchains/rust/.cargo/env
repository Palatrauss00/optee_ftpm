#!/bin/sh
# rustup shell setup
# affix colons on either side of $PATH to simplify matching
case ":${PATH}:" in
    *:"/home/palatrauss/optee_with_ftpm_ta/build/../toolchains/rust/.cargo/bin":*)
        ;;
    *)
        # Prepending path in case a system-installed rustc needs to be overridden
        export PATH="/home/palatrauss/optee_with_ftpm_ta/build/../toolchains/rust/.cargo/bin:$PATH"
        ;;
esac
